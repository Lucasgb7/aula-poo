<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lucas.aula.br.a05controleabastecimento">

    <application
        android:allowBackup="true"
        android:icon="@drawable/gas"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gas"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaPostos" />
        <activity android:name=".AdicionarAbastecimento" />
        <activity android:name=".PostoDetalhes"></activity>
    </application>

</manifest>